```
cd ~ 返回Root用户位置
cd - 返回上一次目录
cd .. 返回上一级目录
cd ~/Desktop 进入桌面位置

find *.txt 找到当前目录下所有的txt文件
ls
ls -l
ls -la

mkdir dirname
touch filename.ext

rm filename.ext
rmdir emptyDirname

ctrl+a 光标到行首
ctrl+e 光标到行尾
ctrl+c 终止进程

command+k 清屏
```